<!DOCTYPE html>
<html lang="ru">
<head>
  <title>Награды — <%= siteTitle %></title>
  <%- include('partials/head') %>
</head>
<body>
  <%- include('partials/header') %>

  <main class="wrap page">
    <p class="eyebrow">Признание</p>
    <h1 class="page-title">Награды</h1>

    <% if (items.length === 0) { %>
      <p class="empty-state">Награды пока не добавлены.</p>
    <% } else { %>
      <ol class="timeline">
        <% items.forEach(function(item, idx) { %>
          <li class="timeline__item reveal" style="transition-delay: <%= Math.min(idx * 70, 420) %>ms">
            <span class="timeline__year"><%= item.year || '' %></span>
            <span class="timeline__dot"></span>
            <div class="timeline__card">
              <% if (item.image) { %>
                <img class="timeline__photo" src="<%= rawUrl(item.image) %>" alt="<%= item.title %>" loading="lazy" />
              <% } %>
              <h3 class="timeline__title"><%= item.title %></h3>
              <% if (item.description) { %><p class="timeline__desc"><%= item.description %></p><% } %>
            </div>
          </li>
        <% }); %>
      </ol>
    <% } %>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
